@page
@model kitap_listeleme_app.Pages.KitapListesi.IndexModel
@{
}
<br />

<div class="container row p-0 m-0">
    <div class="col-9">
        <h2 class="text-info">Kitap Listesi</h2>
    </div>

    <div class="col-3">
        <a asp-page="GuncelleEkle" class="btn btn-info form-control text-white">Yeni Kitap Oluştur</a>
    </div> 

    <div class="col-12 border p-3">
        <table id="DT_load" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Kitap Adı</th>
                    <th>Yazar</th>
                    <th>ISBN</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
        </table>
    </div>

</div>

@section Scripts{ 
<script src="~/js/kitapListesi.js"></script>
}